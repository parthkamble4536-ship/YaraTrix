name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
    
    # Phase 4 will fill in the testing, linting and docker build steps.
    - name: Run placeholder
      run: echo "CI will be implemented in Phase 4"
